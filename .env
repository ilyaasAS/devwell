# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides (clés Stripe, MongoDB Atlas, etc.)
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration
#
# --- Configuration standard pour l'environnement Docker (docker compose) ---

###> symfony/framework-bundle ###
APP_ENV=dev
# Secret de dev uniquement. Mettre le vrai secret dans .env.local ou dans l'env en prod.
APP_SECRET=dev-change-me
APP_DEBUG=1
# Derrière Caddy (reverse proxy HTTPS) : Symfony utilise X-Forwarded-* pour le schéma/host (évite CORS profiler).
TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Hôte "database" = nom du service dans docker-compose (réseau Docker).
# Ne pas utiliser 127.0.0.1 : depuis le conteneur app, la BDD est sur un autre conteneur.
DATABASE_URL="mysql://app:!ChangeMe!@database:3306/devwell?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Mailpit local (service "mailpit" dans docker-compose).
MAILER_DSN="smtp://mailpit:1025"
###< symfony/mailer ###

# --- Stripe (application) ---
# Valeurs d'exemple uniquement. Définir les vraies clés dans .env.local (non versionné).
# STRIPE_PUBLIC_KEY=sk_test
# STRIPE_SECRET_KEY=sk_test

###> doctrine/mongodb-odm-bundle ###
# MongoDB local (service "mongodb" dans docker-compose).
# En prod / staging, .env.local définira MONGODB_URL vers Atlas et écrasera cette valeur.
MONGODB_URL="mongodb://mongodb:27017"
MONGODB_DB=devwell
###< doctrine/mongodb-odm-bundle ###