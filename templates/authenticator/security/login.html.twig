{% extends 'base.html.twig' %} 

{% block title %}Connexion{% endblock %}

{% block body %}
{# Le contenu principal de la page est défini ici. #}

<div class="min-h-screen flex items-center justify-center bg-white py-12 px-4 sm:px-6 lg:px-8">
    {# Conteneur qui occupe toute la hauteur de l'écran (min-h-screen) avec un centrage horizontal et vertical du contenu. 
    Le padding est adapté selon les tailles d'écran (sm, lg). #}

    <div class="max-w-md w-full space-y-8 bg-white p-8 shadow-secondary rounded-lg transform transition-transform duration-300 hover:scale-105 hover:shadow-hover">
        {# Conteneur du formulaire avec un max-width pour qu'il ne dépasse pas une certaine taille. Il a également des effets de transition et de survol. #}

        {# Affichage du message d'erreur personnalisé pour un mot de passe incorrect ou une autre erreur d'authentification #}
        {% if error %}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
            <p>{{ error.messageKey|trans(error.messageData, 'security') }}</p>
        </div>
        {% endif %}
        {# Si une erreur existe (par exemple, un mot de passe incorrect), le message d'erreur est affiché ici avec des classes Tailwind pour un fond rouge et du texte rouge. #}

        {% if error %}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
            <p>Identifiants incorrects. Veuillez réessayer.</p>
        </div>
        {% endif %}
        {# Ce bloc est redondant avec le précédent et pourrait être supprimé pour éviter de répéter le même message d'erreur. #}

        {% for message in app.flashes('success') %}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative">
            <p>{{ message }}</p>
        </div>
        {% endfor %}
        {# Affiche les messages de succès (par exemple, après une inscription réussie) avec des classes Tailwind pour un fond vert et du texte vert. #}

        {% for message in app.flashes('error') %}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
            <p>{{ message }}</p>
        </div>
        {% endfor %}
        {# Affiche les messages d'erreur qui sont définis dans le contrôleur (par exemple, un échec de soumission du formulaire) avec un fond rouge. #}

        <h2 class="text-center text-2xl font-extrabold text-primary_dw">Connectez-vous</h2>
        {# Titre principal du formulaire centré et stylisé en utilisant Tailwind (taille de police, poids de police, couleur personnalisée). #}

        <form class="mt-8 space-y-6" method="post">
            {# Formulaire qui soumet une requête POST pour la connexion. Le padding entre les éléments est géré par space-y-6. #}

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            {# Champ caché pour le jeton CSRF afin de sécuriser le formulaire contre les attaques de type CSRF. #}

            <!-- Champs email et password -->
            <div class="rounded-md shadow-sm -space-y-px">
                {# Conteneur pour les champs de formulaire avec une bordure arrondie et un petit espacement entre les champs. #}

                <div>
                    <label for="inputEmail" class="sr-only">Email</label>
                    {# L'étiquette 'Email' est masquée visuellement (sr-only) mais reste accessible aux lecteurs d'écran. #}

                    <input id="inputEmail" name="email" type="email" value="{{ last_username }}" autocomplete="email" required 
                        class="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-text_dw text-text_dw rounded-t-md focus:outline-none focus:ring-secondary_dw focus:border-secondary_dw sm:text-sm transition-all duration-300 hover:scale-105"
                        placeholder="Email">
                    {# Champ pour l'email avec des styles Tailwind pour l'apparence (border, padding, focus, transition). Il est pré-rempli avec `last_username` (email précédemment saisi). #}
                </div>

                <div>
                    <label for="inputPassword" class="sr-only">Password</label>
                    {# L'étiquette 'Password' est également masquée pour l'accessibilité. #}

                    <input id="inputPassword" name="password" type="password" autocomplete="current-password" required 
                        class="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-text_dw text-text_dw rounded-b-md focus:outline-none focus:ring-secondary_dw focus:border-secondary_dw sm:text-sm transition-all duration-300 hover:scale-105"
                        placeholder="Password">
                    {# Champ pour le mot de passe avec les mêmes classes Tailwind que l'email, mais avec des styles spécifiques pour le bas du champ (rounded-b-md). #}
                </div>
            </div>

            <!-- Souvenez-vous de moi + Lien mot de passe oublié -->
            <div class="flex items-center justify-between">
                {# Conteneur pour les options "Se souvenir de moi" et le lien de mot de passe oublié. Utilise un layout flex pour les aligner horizontalement. #}

                <div class="flex items-center">
                    <input id="remember_me" name="_remember_me" type="checkbox" 
                        class="h-4 w-4 text-primary_dw focus:ring-secondary_dw border-gray-300 rounded">
                    {# Case à cocher pour "Se souvenir de moi", avec une taille spécifique et un style au survol. #}
                    <label for="remember_me" class="ml-2 block text-sm text-text_dw">
                        Se souvenir de moi
                    </label>
                </div>

                <div class="text-sm">
                    <a href="{{ path('app_forgot_password_request') }}" class="font-medium text-primary_dw hover:text-secondary_dw transition-all duration-300 transform hover:scale-105">
                        Mot de passe oublié
                    </a>
                    {# Lien vers la page de récupération du mot de passe, avec des effets de transition au survol. #}
                </div>
            </div>

            <!-- Bouton de soumission -->
            <div>
                <button type="submit" 
                    class="inline-block bg-primary_dw text-tertiary_dw px-6 py-3 rounded-lg shadow-md hover:bg-secondary_dw hover:text-primary_dw focus:bg-secondary_dw focus:text-primary_dw transition duration-300 transform hover:scale-105 w-full text-center mb-4">
                    Connexion
                </button>
                {# Le bouton de soumission du formulaire, avec des classes pour l'apparence, la couleur, l'effet de survol et de focus. #}
            </div>
        </form>
    </div>
</div>

{% endblock %}
{# Fin du bloc `body` #}
