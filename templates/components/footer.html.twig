<footer style="background-color: #1a2a40;" class="text-white py-6">
    <div class="container mx-auto px-4">
        <div class="flex flex-col sm:flex-row justify-between items-center">
            <!-- Liens du footer -->
            <nav class="mb-4 sm:mb-0">
                <ul class="flex flex-col sm:flex-row space-y-2 sm:space-y-0 sm:space-x-6 text-sm">
                    <li>
                        <a href="{{ path('app_cgv') }}" 
                           class="hover:text-secondary_dw hover:underline cursor-pointer transition duration-300">
                            CGV
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_cgu') }}" 
                           class="hover:text-secondary_dw hover:underline cursor-pointer transition duration-300">
                            CGU
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_mentions_legales') }}" 
                           class="hover:text-secondary_dw hover:underline cursor-pointer transition duration-300">
                            Mentions Légales
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_contact') }}" 
                           class="hover:text-secondary_dw hover:underline cursor-pointer transition duration-300">
                            Contact
                        </a>
                    </li>
                </ul>
            </nav>

            <!-- Copyright -->
            <div class="text-sm text-gray-400">
                &copy; {{ "now"|date("Y") }} VotreEntreprise. Tous droits réservés.
            </div>
        </div>
    </div>
</footer>
