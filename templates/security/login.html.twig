{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-tertiary_dw py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8 bg-white p-8 shadow-md rounded-lg">
        {% if error %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}

        <h2 class="text-center text-2xl font-extrabold text-primary_dw">Please sign in</h2>

        <form class="mt-8 space-y-6" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <div class="rounded-md shadow-sm -space-y-px">
                <div>
                    <label for="inputEmail" class="sr-only">Email</label>
                    <input id="inputEmail" name="email" type="email" value="{{ last_username }}" autocomplete="email" required 
                        class="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-text_dw text-text_dw rounded-t-md focus:outline-none focus:ring-primary_dw focus:border-primary_dw sm:text-sm"
                        placeholder="Email">
                </div>
                <div>
                    <label for="inputPassword" class="sr-only">Password</label>
                    <input id="inputPassword" name="password" type="password" autocomplete="current-password" required 
                        class="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-text_dw text-text_dw rounded-b-md focus:outline-none focus:ring-primary_dw focus:border-primary_dw sm:text-sm"
                        placeholder="Password">
                </div>
            </div>

            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <input id="remember_me" name="_remember_me" type="checkbox" 
                        class="h-4 w-4 text-primary_dw focus:ring-primary_dw border-gray-300 rounded">
                    <label for="remember_me" class="ml-2 block text-sm text-text_dw">
                        Remember me
                    </label>
                </div>
                <div class="text-sm">
                    <a href="#" class="font-medium text-primary_dw hover:text-secondary_dw">Forgot your password?</a>
                </div>
            </div>

            <div>
                <button type="submit" 
                    class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-primary_dw hover:bg-secondary_dw focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary_dw">
                    Sign in
                </button>
            </div>
        </form>
    </div>
</div>
{% endblock %}
