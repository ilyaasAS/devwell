{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8 bg-white p-8 rounded-lg shadow-lg transform transition-transform duration-300 hover:scale-105">
        
        <h2 class="text-center text-2xl font-extrabold text-primary_dw">Réinitialisation du mot de passe</h2>
        
        <p class="text-center text-lg text-gray-700 mt-4">
            Si un compte correspondant à votre adresse électronique existe, un e-mail contenant un lien pour réinitialiser votre mot de passe a été envoyé. Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
        </p>

        <p class="text-center text-sm text-gray-500 mt-4">
            Si vous ne recevez pas l'e-mail, vérifiez votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}" class="font-medium text-primary_dw hover:text-secondary_dw transition-all duration-300 transform hover:scale-105">essayez à nouveau</a>.
        </p>
        
    </div>
</div>
{% endblock %}
